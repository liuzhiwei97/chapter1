eureka:
  client:
    serviceUrl:
    #注册地址
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
  #spring.application.name,这个很重要，这在以后的服务与服务之间相互调用一般都是根据这个name
    name: service-hi